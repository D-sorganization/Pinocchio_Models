# D — Testing (Weight: 10%)

## Score: 7 / 10

## Evidence

- **Test count**: 4358 lines across 39 test files; pytest collects 480+ test cases.
- **Test categories**: unit (290+), integration (50), parity (25), benchmarks (5).
- **Coverage config**: `fail_under = 80`, branch coverage enabled, `show_missing = true`.
- **Markers**: `@pytest.mark.slow`, `@pytest.mark.integration`, `@pytest.mark.unit`, `@pytest.mark.benchmark`, `@pytest.mark.requires_pinocchio`.
- **Fixtures/mocks**: 92 occurrences of parametrize/fixture/mock patterns.
- **Property-based**: Hypothesis tests present (`tests/unit/shared/test_geometry_hypothesis.py`).
- **CI matrix**: Python 3.10, 3.11, 3.12 with `--cov-fail-under=80`.
- **Test error**: `test_geometry_hypothesis.py` fails collection due to `ImportError: cannot import name 'given' from 'hypothesis'` — hypothesis package not installed in this environment but present in `pyproject.toml` dev deps.

## Findings

### P0
- **`test_geometry_hypothesis.py` collection failure** — ImportError blocks full test suite execution in environments without hypothesis.
  - File: `tests/unit/shared/test_geometry_hypothesis.py:6`

### P1
- **No mutation testing** — No `mutmut` or similar tool to verify test quality beyond line coverage.
- **Benchmark tests disabled under xdist** — `pytest_benchmark` warns: "Benchmarks are automatically disabled because xdist plugin is active." Benchmarks never run in CI as configured.
  - File: `tests/benchmarks/test_model_generation_benchmark.py`

### P2
- **No fuzzing beyond hypothesis** — No `atheris` or dedicated fuzz harness for URDF generation edge cases.
